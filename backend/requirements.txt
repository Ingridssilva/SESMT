flask==3.0.3
flask-cors==4.0.1
psycopg2-binary==2.9.9
python-dotenv==1.0.1
reportlab==4.2.2
gunicorn==22.0.0
PyJWT==2.8.0
cryptography==42.0.8
requests==2.32.3

# ── Testes (não instalar em produção) ─────────────────────────────────────────
# pip install -r requirements.txt --extra-index-url ... ou:
# pytest e pytest-flask instalados separadamente no ambiente de dev
